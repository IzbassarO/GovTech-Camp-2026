"""P1 — Document Integrity deterministic baseline (no LLM).

The output score is ``document_integrity_priority_score`` (0-100): a manual
review PRIORITY for document structure, never a probability of violation.
"""

P1_VERSION = "1.0.0"
PILLAR_ID = "P1"
